package sources;

public class SourceFix extends Source<Boolean> {

	public SourceFix (String message) {
		
		super();
		
		Boolean[] informationBool = new Boolean[message.length()];
		
		for (int i=0 ; i < message.length() ; i++ ) {
			if ( message.charAt(i) == 0 ) {
				
			}
		}
		
		
	}
	
}
